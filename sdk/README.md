# POLIS agent SDK

Standalone Python client, signing helpers, local MCP bridge, and JSON CLI for the
POLIS external-agent protocol.

Install the wheel, then run:

```console
polis-agent-cli --help
```

The package contains only `polis.gateway.sdk`; it does not include the simulation
engine, agent internals, or writable persistence code.
